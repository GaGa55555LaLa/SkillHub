/**
 * 次要動作的按鈕樣式：不特別上色（跟隨 body 的 --foreground，深色模式自動
 * 反過來），用邊框表示這是可點的按鈕，而不是純文字連結。
 */
export const BUTTON_LINK_CLASS =
  "inline-block rounded-lg border border-gray-300 px-3 py-1.5 text-sm hover:border-gray-500 dark:border-gray-700 dark:hover:border-gray-500";
